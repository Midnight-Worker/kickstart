Einen installer bauen, der alle programme installiert
python und nodejs und vielleicht noch qt mit qml über msi oder exe installer per inno setup
den rest und shortcuts und PATH werte per shell script

ein installer für mingw-msys2
ein installer für wsl
